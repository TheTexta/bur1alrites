services:
  bur1alrites-hls-worker:
    build: .
    container_name: bur1alrites-hls-worker
    restart: unless-stopped
    env_file:
      - .env
    environment:
      HLS_POLL_INTERVAL_SECONDS: "300"
      HLS_SEGMENT_SECONDS: "4"
      HLS_FFMPEG_PRESET: "slow"
    cpus: 2
    mem_limit: 4g
